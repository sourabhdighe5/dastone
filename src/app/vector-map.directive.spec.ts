import { VectorMapDirective } from './vector-map.directive';

describe('VectorMapDirective', () => {
  it('should create an instance', () => {
    const directive = new VectorMapDirective();
    expect(directive).toBeTruthy();
  });
});
